# ❓ FAQ

**Why are README images dark?** Star-field readability — intentional (`hero-orbits.png`).

**Do I need a server for the dashboard?** No — open `app/index.html` directly.

**How do I change detection threshold?** `detect(flux, threshold=...)` or dashboard slider.

**Real TLEs?** Roadmap v1.4 — currently circular J2 model.

**Python version?** 3.11+ (tested 3.11–3.13).
